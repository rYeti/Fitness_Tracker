enum Sex { male, female, other }

enum GoalType { weightLoss, muscleGain, maintenance }

enum ActivityLevel {
  sedentary,
  lightlyActive,
  moderatelyActive,
  veryActive,
  extremelyActive,
}

enum MediaType {
  picture,
  video
}
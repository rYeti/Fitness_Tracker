{
  "name": "forgeform-e2e",
  "private": true,
  "version": "0.0.0",
  "description": "Playwright end-to-end tests for the ForgeForm Flutter web bundle (Trainer Console).",
  "type": "module",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "report": "playwright show-report",
    "serve": "node tools/serve-web.mjs"
  },
  "devDependencies": {
    "@playwright/test": "^1.56.1",
    "@types/node": "^26.3.0",
    "typescript": "^7.0.2"
  }
}
